import tkinter as tk

class HomePage:
    def __init__(self, master):
        self.master = master
        self.master.title("Home Page")

        self.label = tk.Label(master, text="Boolean Algebra Simplifier", font=('Helvetica', 18))
        self.label.pack(pady=20)

        # Buttons to navigate to other pages
        self.button1 = tk.Button(master, text="Boolean Algebra Simplifier", command=self.open_page1)
        self.button1.pack(pady=10)

        self.button2 = tk.Button(master, text="Logic Gates Simplifier", command=self.open_page2)
        self.button2.pack(pady=10)

        self.button3 = tk.Button(master, text="Boolean Algebra Formulas", command=self.open_page3)
        self.button3.pack(pady=10)

        self.button4 = tk.Button(master, text="Logic Gates Explanation", command=self.open_page4)
        self.button4.pack(pady=10)
        root.mainloop()


    def open_page1(self):
        # Add code to handle navigation to Page 1
        print("Navigating to Page 1")

    def open_page2(self):
        # Add code to handle navigation to Page 2
        print("Navigating to Page 2")

    def open_page3(self):
        # Add code to handle navigation to Page 3
        print("Navigating to Page 3")

    def open_page4(self):
        # Add code to handle navigation to Page 4
        print("Navigating to Page 4")




if __name__ == "__main__":
    root = tk.Tk()
    home_page = HomePage(root)
